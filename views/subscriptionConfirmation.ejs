<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shop-Cart | Subscription Confirmation</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;500;700&display=swap"
      rel="stylesheet"
    />

    <!-- Font Awesome Icons -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        font-family: "Manrope", sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
        background: linear-gradient(to bottom right, #f0f2f5, #dfe4ea);
      }

      .container {
        max-width: 450px;
        padding: 40px;
        background-color: #ffffff;
        box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.15);
        border-radius: 15px;
        text-align: center;
        position: relative;
      }

      .container img {
        max-width: 100%;
        margin-bottom: 25px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      h1 {
        color: #2d3436;
        font-size: 24px;
        margin: 0;
        font-weight: 700;
      }

      p {
        color: #636e72;
        font-size: 16px;
        margin-top: 10px;
        line-height: 1.6;
      }

      .subscribe-button {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 24px;
        font-size: 16px;
        font-weight: 500;
        color: #ffffff;
        background-color: #0984e3;
        border: none;
        border-radius: 25px;
        cursor: pointer;
        text-decoration: none;
        transition: background-color 0.3s ease;
      }

      .subscribe-button:hover {
        background-color: #74b9ff;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <img
        src="https://thumbs.dreamstime.com/b/website-under-construction-sign-illustration-background-36399059.jpg"
        alt="Under Construction"
      />
      <h1><%= successMessage %></h1>
      <p><%= infoMessage %></p>
      <a href="/" id="dashboard-link" class="subscribe-button">
        <i class="fas fa-home"></i> Back to Home
      </a>
    </div>
    <script>
      const dashboardLink = document.getElementById("dashboard-link");
      const userRole = "<%= userRole %>";

      if (userRole === "user") {
        dashboardLink.href = "/user/dashboard";
      } else if (userRole === "admin") {
        dashboardLink.href = "/admin/dashboard";
      } else {
        dashboardLink.href = "/";
      }
    </script>
  </body>
</html>
